spring:
    profiles:
        active: dev
    banner:
        charset: UTF-8
        location: classpath:robot-inventory-banner.txt
    datasource:
        url: jdbc:h2:mem:robotinventory
        username: icaxdb
        password: Dev1testDB
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect # h2 dialect for auto-generated scripts (maps jdbc type to db type)
        show-sql: true
        #properties:
            # hibernate.hbm2ddl.auto: validate # Enable in higher env when using persistence DB for better control

server:
    servlet:
        context-path: /api/v1
    error:
        include-stacktrace: never